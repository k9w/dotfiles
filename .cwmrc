# Remove window border for applications to correctly fill the screen.

borderwidth 0

# cwm(1) uses C-/ to search applications. Emacs and mg use it to undo,
# which is what I want. Unbind it from cwm with C-slash per manpage. 

unbind-key C-slash

